<% layout = false %>
<!DOCTYPE html>
<html lang="<%= lng %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= t('common:app_name') %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="/css/modern_quiz_list.css" rel="stylesheet">
    <link href="/css/quiz_list_number.css" rel="stylesheet">
    <!-- SweetAlert2 -->
    <link href="https://cdn.jsdelivr.net/npm/@sweetalert2/theme-bootstrap-4/bootstrap-4.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/quizzes">
                <i class="fas fa-brain"></i>
                <%= t('common:app_name') %>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- Language Switcher -->
                <ul class="navbar-nav me-auto">
                    <!-- Room Selection Info -->
                    <% if (typeof roomInfo !== 'undefined' && roomInfo) { %>
                    <li class="nav-item dropdown ms-3">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" 
                           href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="d-flex align-items-center">
                                <div class="d-flex flex-column">
                                    <span class="fw-medium d-none d-md-inline" style="font-size: 0.9rem;">
                                        <%= roomInfo.name %>
                                    </span>
                                    <span class="fw-medium d-md-none">
                                        <%= roomInfo.code.toUpperCase() %>
                                    </span>
                                    <small class="text-white-50 d-none d-lg-inline" style="font-size: 0.75rem;">
                                        <%= t('quiz:selected') %>: <%= new Date(roomInfo.selectedAt).toLocaleTimeString(lng === 'vi' ? 'vi-VN' : 'en-US', { hour12: false })%>
                                    </small>
                                </div>
                            </div>
                        </a>
                        <ul class="dropdown-menu">
                            <li>
                                <h6 class="dropdown-header">
                                    <i class="fas fa-building me-2"></i>
                                    <%= t('quiz:current_department') %>
                                </h6>
                            </li>
                            <li>
                                <span class="dropdown-item-text">
                                    <strong><%= roomInfo.name %></strong><br>
                                    <small class="text-muted"><%= t('quiz:code') %>: <%= roomInfo.code.toUpperCase() %></small>
                                </span>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="#" onclick="switchDepartment()">
                                    <i class="fas fa-exchange-alt me-2"></i><%= t('quiz:switch_department') %>
                                </a>
                            </li>
                        </ul>
                    </li>
                    <% } %>
                </ul>
                
                <div class="navbar-nav ms-auto">
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" 
                           href="#" 
                           role="button" 
                           data-bs-toggle="dropdown" 
                           aria-expanded="false"
                           id="navbarDropdown">
                            <div class="d-flex align-items-center">
                                <div class="rounded-circle bg-white bg-opacity-20 p-2 me-2" style="position: relative;">
                                    <img src="https://jbagy.me/wp-content/uploads/2025/03/anh-dai-dien-zalo-dep-1.jpg" alt="User" class="rounded-circle align-avatar-image" style="object-fit: cover;">
                                </div>
                                <span class="fw-medium d-none d-sm-inline">
                                    <% if (typeof user !== 'undefined' && user) { %>
                                        <%= user.name || t('quiz:admin') %>
                                    <% } else { %>
                                        <%= t('quiz:admin') %>
                                    <% } %>
                                </span>
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="navbarDropdown">
                            <li>
                                <h6 class="dropdown-header">
                                    <% if (typeof user !== 'undefined' && user) { %>
                                        <i class="fas fa-envelope me-2"></i>
                                        <%= user.email %>
                                    <% } %>
                                </h6>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="#"><i class="fas fa-user me-2"></i><%= t('common:profile') %></a></li>
                            <li><a class="dropdown-item" href="#"><i class="fas fa-cog me-2"></i><%= t('common:settings') %></a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/auth/logout"><i class="fas fa-sign-out-alt me-2"></i><%= t('common:logout') %></a></li>
                        </ul>
                    </div>
                </div>
                <div class="language-switcher-container" style="text-align: end;">
                    <%- include('../partials/language-switcher') %>
                </div>
            </div>
        </div>
    </nav>

    <div class="main-container">
        <!-- Header Section -->
        <div class="page-header">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-lg-12">
                        <div class="header-content">
                            <h1 class="page-title">
                                <i class="fas fa-clipboard-list me-3"></i>
                                <%= t('quiz:management') %>
                                <% if (typeof roomInfo !== 'undefined' && roomInfo) { %>
                                <small class="text-white-50 d-block mt-2" style="font-size: 1.2rem; font-weight: 500;">
                                    <i class="fas <%= roomInfo.roomCode === 'hrm' ? 'fa-users' : roomInfo.roomCode === 'hse' ? 'fa-shield-alt' : roomInfo.roomCode === 'qasx' ? 'fa-clipboard-check' : roomInfo.roomCode === 'sale-marketing' ? 'fa-bullhorn' : 'fa-crown' %> me-2"></i>
                                    <%= roomInfo.name %> <%= t('quiz:department') %>
                                </small>
                                <% } %>
                            </h1>
                        </div>
                    </div>
                    <div class="col-lg-12 text-lg-start">
                        <div class="header-actions">
                            <a href="/quizzes/create" class="btn btn-primary-custom me-2" style="margin-top: 10px;">
                                <i class="fas fa-plus me-2"></i>
                                <%= t('quiz:create_new') %>
                            </a>
                            <button class="btn btn-warning-custom me-2" onclick="openCreateTestModal()" style="margin-top: 10px;">
                                <i class="fas fa-rocket me-2"></i>
                                <%= t('test:create_test') %>
                            </button>
                            <a href="/test/admin/results" class="btn btn-result-custom me-2" style="margin-top: 10px; margin-right: 0 !important;">
                                <i class="fas fa-chart-bar me-2"></i>
                                <%= t('test:test_results') %>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Filter Section -->
        <div class="filter-section">
            <div class="container">
                <div class="filter-card">
                    <div class="filter-header">
                        <h5 class="filter-title">
                            <i class="fas fa-filter me-2"></i>
                            <%= t('quiz:filter_and_search') %>
                            <% if (typeof roomInfo !== 'undefined' && roomInfo) { %>
                            <small class="text-muted">(<%= roomInfo.code.toUpperCase() %> <%= t('quiz:department') %>)</small>
                            <% } %>
                        </h5>
                        <button class="btn btn-sm btn-outline-secondary" onclick="clearFilters()">
                            <i class="fas fa-times me-1"></i>
                            <%= t('quiz:clear_all') %>
                        </button>
                    </div>
                    <div class="filter-body">
                        <div class="row g-3">
                            <div class="col-lg-4 col-md-6">
                                <div class="search-wrapper">
                                    <i class="fas fa-search search-icon"></i>
                                    <input type="text" class="form-control search-input" id="searchQuiz" 
                                           placeholder="<%= t('quiz:search_placeholder') %>">
                                </div>
                            </div>
                            <div class="col-lg-2 col-md-3 col-sm-6">
                                <select class="form-select filter-select" id="filterMode">
                                    <option value=""><%= t('quiz:all_modes') %></option>
                                    <option value="online"><%= t('quiz:online') %></option>
                                    <option value="offline"><%= t('quiz:offline') %></option>
                                </select>
                            </div>
                            <div class="col-lg-2 col-md-3 col-sm-6">
                                <select class="form-select filter-select" id="filterLanguage" disabled>
                                    <option value=""><%= t('quiz:all_languages') %></option>
                                    <option value="vietnamese"><%= t('quiz:vietnamese') %></option>
                                    <option value="english"><%= t('quiz:english') %></option>
                                </select>
                            </div>
                            <div class="col-lg-2 col-md-6 col-sm-6">
                                <select class="form-select filter-select" id="sortBy">
                                    <option value="newest"><%= t('quiz:newest_first') %></option>
                                    <option value="oldest"><%= t('quiz:oldest_first') %></option>
                                    <option value="name"><%= t('quiz:name_a_z') %></option>
                                </select>
                            </div>
                            <div class="col-lg-2 col-md-6 col-sm-6">
                                <div class="view-toggle">
                                    <button class="view-btn active" data-view="grid" title="<%= t('quiz:grid_view') %>" disabled>
                                        <i class="fas fa-th"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quizzes Section -->
        <div class="quizzes-section">
            <div class="container">
                <!-- Loading State -->
                <div id="loadingState" class="loading-state" style="display: none;">
                    <div class="loading-spinner">
                        <i class="fas fa-spinner fa-spin"></i>
                    </div>
                    <p><%= t('quiz:loading_quizzes') %></p>
                </div>

                <!-- Empty State -->
                <% if (quizzes.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-icon">
                        <i class="fas fa-clipboard-list"></i>
                    </div>
                    <h3><%= t('quiz:no_quizzes_yet') %></h3>
                    <p>
                        <% if (typeof roomInfo !== 'undefined' && roomInfo) { %>
                            <%= ti('quiz:get_started_department', { department: roomInfo.name }) %>
                        <% } else { %>
                            <%= t('quiz:get_started_general') %>
                        <% } %>
                    </p>
                    <a href="/quizzes/create" class="btn btn-primary-custom">
                        <i class="fas fa-plus me-2"></i>
                        <%= t('quiz:create_first_quiz') %>
                    </a>
                </div>
                <% } else { %>

                <!-- Quiz Grid -->
                <div class="quiz-grid" id="quizGrid">
                    <% quizzes.forEach((quiz, index) => { %>
                    <div class="quiz-card" 
                         data-mode="<%= quiz.mode %>" 
                         data-language="<%= quiz.language %>"
                         data-title="<%= quiz.title.toLowerCase() %>"
                         data-number="<%= quiz.number || index + 1 %>"
                         data-date="<%= quiz.updatedAt %>">
                        
                        <!-- Card Header -->
                        <div class="card-header">
                            <div class="card-badges">
                                <span class="badge badge-<%= quiz.mode %>">
                                    <i class="fas <%= quiz.mode === 'online' ? 'fa-globe' : 'fa-desktop' %> me-1"></i>
                                    <%= t('quiz:' + quiz.mode) %>
                                </span>
                                <!-- Department Badge -->
                                <% if (quiz.roomCode) { %>
                                <span class="badge badge-language" style="background: linear-gradient(135deg, var(--warning-color), #d97706);">
                                    <i class="fas <%= quiz.roomCode === 'hrm' ? 'fa-users' : quiz.roomCode === 'hse' ? 'fa-shield-alt' : quiz.roomCode === 'qasx' ? 'fa-clipboard-check' : quiz.roomCode === 'sale-marketing' ? 'fa-bullhorn' : 'fa-crown' %> me-1"></i>
                                    <%= quiz.roomCode.toUpperCase() %>
                                </span>
                                <% } %>
                            </div>
                            <div class="card-actions">
                                <div class="dropdown">
                                    <button class="action-btn" 
                                            type="button"
                                            onclick="deleteQuiz('<%= quiz._id %>')"">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Card Body -->
                        <div class="card-body">
                            <h3 class="quiz-title" title="<%= quiz.title %>">
                                <%= quiz.title %>
                            </h3>
                            
                            <!-- Quiz Stats -->
                            <div class="quiz-stats">
                                <div class="stat">
                                    <i class="fas fa-list-ol"></i>
                                    <span><%= quiz.number || (index + 1) %></span>
                                </div>
                                <div class="stat">
                                    <i class="fas fa-users"></i>
                                    <span><%= quiz.totalCount || 0 %> <%= t('quiz:participants') %></span>
                                </div>
                                <% if (quiz.scheduleSettings) { %>
                                <div class="stat">
                                    <i class="fas fa-calendar"></i>
                                    <span><%= t('quiz:scheduled') %></span>
                                </div>
                                <% } %>
                                <div class="stat">
                                    <i class="fas fa-clock"></i>
                                    <span><%= quiz.estimatedDuration || '0m' %></span>
                                </div>
                            </div>

                            <!-- Card Footer -->
                            <div class="card-footer">
                                <span class="meta-item">
                                    <i class="fas fa-clock me-1"></i>
                                    <%= t('quiz:updated') %> <%= new Date(quiz.updatedAt).toLocaleDateString() %>
                                </span>
                                <div class="quiz-meta">
                                </div>
                                <div class="quick-actions">
                                    <a href="/quizzes/<%= quiz._id %>/preview" class="btn btn-sm btn-outline-primary">
                                        <i class="fas fa-eye me-1"></i>
                                        <%= t('quiz:preview') %>
                                    </a>
                                    <a href="/quizzes/<%= quiz._id %>/edit" class="btn btn-sm btn-primary">
                                        <i class="fas fa-edit me-1"></i>
                                        <%= t('common:edit') %>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% }); %>
                </div>
                <% } %>

                <!-- No Results State -->
                <div id="noResults" class="empty-state" style="display: none;">
                    <div class="empty-icon">
                        <i class="fas fa-search"></i>
                    </div>
                    <h3><%= t('quiz:no_quizzes_found') %></h3>
                    <p><%= t('quiz:try_adjusting_criteria') %></p>
                    <button class="btn btn-outline-primary" onclick="clearFilters()">
                        <i class="fas fa-filter me-2"></i>
                        <%= t('quiz:clear_filters') %>
                    </button>
                </div>
                
                <!-- Pagination Section -->
                <% if (totalPages >= 1) { %>
                    <nav>
                        <ul class="pagination pagination-custom">
                            <!-- Previous Button -->
                            <% if (pagination.hasPrev) { %>
                            <li class="page-item">
                                <a class="page-link" href="?page=<%= pagination.prevPage %>">
                                    <i class="fas fa-chevron-left"></i>
                                </a>
                            </li>
                            <% } %>
                            
                            <!-- Page Numbers -->
                            <% pagination.pages.forEach(page => { %>
                            <li class="page-item <%= page === currentPage ? 'active' : '' %>">
                                <a class="page-link" href="?page=<%= page %>"><%= page %></a>
                            </li>
                            <% }); %>
                            
                            <!-- Next Button -->
                            <% if (pagination.hasNext) { %>
                            <li class="page-item">
                                <a class="page-link" href="?page=<%= pagination.nextPage %>">
                                    <i class="fas fa-chevron-right"></i>
                                </a>
                            </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Create Test Modal -->
    <div class="modal fade" id="createTestModal" tabindex="-1" aria-labelledby="createTestModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header" style="background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%); color: white;">
                    <h5 class="modal-title" id="createTestModalLabel">
                        <i class="fas fa-rocket me-2"></i><%= t('test:create_new_test_session') %>
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="<%= t('common:close') %>"></button>
                </div>
                <div class="modal-body">
                    <form id="createTestForm">
                        <!-- Quiz Selection -->
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="card bg-light border-0">
                                    <div class="card-body">
                                        <h6 class="card-title text-primary mb-3">
                                            <i class="fas fa-list-ol me-2"></i><%= t('test:quiz_selection') %>
                                        </h6>
                                        <div class="form-floating">
                                            <input type="number" 
                                                class="form-control" 
                                                id="quizNumber" 
                                                name="quizNumber" 
                                                placeholder="<%= t('test:quiz_number') %>"
                                                min="1"
                                                required>
                                            <label for="quizNumber"><%= t('test:quiz_number') %></label>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="card-body">
                            <!-- Test Mode -->
                            <div class="row mb-4">
                                <div class="col-12">
                                    <h6 class="text-primary mb-3">
                                        <i class="fas fa-cog me-2"></i><%= t('test:test_mode') %>
                                    </h6>
                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="form-check">
                                                <input class="form-check-input" 
                                                    type="radio" 
                                                    name="mode" 
                                                    id="modeOnline" 
                                                    value="online" 
                                                    checked>
                                                <label class="form-check-label" for="modeOnline">
                                                    <div class="d-flex align-items-center">
                                                        <div>
                                                            <i class="fas fa-globe text-success me-2"></i>
                                                            <strong><%= t('test:online_realtime') %></strong>
                                                        </div>
                                                    </div>
                                                </label>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-check">
                                                <input class="form-check-input" 
                                                    type="radio" 
                                                    name="mode" 
                                                    id="modeOffline" 
                                                    value="offline">
                                                <label class="form-check-label" for="modeOffline">
                                                    <div class="d-flex align-items-center">
                                                        <div>
                                                            <i class="fas fa-clock text-warning me-2"></i>
                                                            <strong><%= t('test:offline_scheduled') %></strong>
                                                        </div>
                                                    </div>
                                                </label>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Schedule Settings (for Offline mode) -->
                            <div class="row mb-4" id="scheduleSettings" style="display: none;">
                                <div class="col-12">
                                    <div class="card border-warning bg-warning bg-opacity-10">
                                        <div class="card-body">
                                            <h6 class="card-title text-warning mb-3">
                                                <i class="fas fa-calendar-alt me-2"></i><%= t('test:schedule_settings') %>
                                            </h6>
                                            <div class="row">
                                                <div class="col-md-6">
                                                    <div class="form-floating mb-3">
                                                        <input type="datetime-local" 
                                                            class="form-control" 
                                                            id="startTime" 
                                                            name="startTime">
                                                        <label for="startTime"><%= t('test:start_time') %></label>
                                                    </div>
                                                </div>
                                                <div class="col-md-6">
                                                    <div class="form-floating mb-3">
                                                        <input type="datetime-local" 
                                                            class="form-control" 
                                                            id="endTime" 
                                                            name="endTime">
                                                        <label for="endTime"><%= t('test:end_time') %></label>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Max Participants -->
                            <div class="row mb-4">
                                <div class="col-12">
                                    <h6 class="text-primary mb-3">
                                        <i class="fas fa-users me-2"></i><%= t('test:participant_limit') %>
                                    </h6>
                                    <div class="form-floating">
                                        <input type="number" 
                                            class="form-control" 
                                            id="maxParticipants" 
                                            name="maxParticipants" 
                                            placeholder="<%= t('test:max_participants') %>"
                                            min="1"
                                            max="1000"
                                            value="50"
                                            required>
                                        <label for="maxParticipants"><%= t('test:max_participants') %></label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-2"></i><%= t('common:cancel') %>
                    </button>
                    <button type="submit" form="createTestForm" class="btn btn-primary" id="createTestBtn">
                        <i class="fas fa-rocket me-2"></i><%= t('test:create_test') %>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Test Created Success Modal -->
    <div class="modal fade" id="testCreatedModal" tabindex="-1" aria-labelledby="testCreatedModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title" id="testCreatedModalLabel">
                        <i class="fas fa-check-circle me-2"></i><%= t('test:test_created_successfully') %>
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="<%= t('common:close') %>"></button>
                </div>
                <div class="modal-body" id="testCreatedContent">
                    <!-- Content will be populated by JavaScript -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common:close') %></button>
                    <button type="button" class="btn btn-primary" id="startTestBtn" style="display: none;">
                        <i class="fas fa-play me-2"></i><%= t('test:start_test_now') %>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6 text-center text-md-start">
                    <span class="footer-text">
                        <i class="fas fa-brain me-2"></i>
                        <%= t('common:app_name') %> © <%= new Date().getFullYear() %> <%= t('quiz:all_rights_reserved') %>
                        <% if (typeof roomInfo !== 'undefined' && roomInfo) { %>
                        <small class="text-muted ms-2">
                            | <%= roomInfo.name %> <%= t('quiz:department') %>
                        </small>
                        <% } %>
                    </span>
                </div>
                <div class="col-md-6">
                    <div class="footer-links">
                        <a href="/about"><%= t('quiz:about') %></a>
                        <a href="/privacy"><%= t('quiz:privacy') %></a>
                        <a href="/terms"><%= t('quiz:terms') %></a>
                        <a href="/contact"><%= t('quiz:contact') %></a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/modern_quiz_list.js"></script>
    
    <!-- Translation object for JavaScript -->
    <script>
        window.translations = {
            lng: '<%= lng %>',
            switchDepartment: '<%= t("quiz:switch_department") %>',
            switchDepartmentConfirm: '<%= t("quiz:switch_department_confirm") %>',
            switchDepartmentText: '<%= t("quiz:switch_department_text") %>',
            yesSwitchBtn: '<%= t("quiz:yes_switch") %>',
            cancelBtn: '<%= t("common:cancel") %>',
            errorTitle: '<%= t("common:error") %>',
            errorSwitchDepartment: '<%= t("quiz:error_switch_department") %>',
            loadingQuizzes: '<%= t("quiz:loading_quizzes") %>',
            quizzesRefreshed: '<%= t("quiz:quizzes_refreshed") %>',
            deleteQuizTitle: '<%= t("quiz:delete_quiz_title") %>',
            deleteQuizText: '<%= t("quiz:delete_quiz_text") %>',
            yesDeleteBtn: '<%= t("quiz:yes_delete") %>',
            deletingQuiz: '<%= t("quiz:deleting_quiz") %>',
            pleaseWait: '<%= t("quiz:please_wait") %>',
            quizDeleted: '<%= t("quiz:quiz_deleted") %>',
            quizDeletedText: '<%= t("quiz:quiz_deleted_text") %>',
            failedDeleteQuiz: '<%= t("quiz:failed_delete_quiz") %>',
            creatingTest: '<%= t("test:creating_test") %>',
            errorCreatingTest: '<%= t("test:error_creating_test") %>',
            createTest: '<%= t("test:create_test") %>',
            testCode: '<%= t("test:test_code") %>',
            schedule: '<%= t("test:schedule") %>',
            mode: '<%= t("test:mode") %>',
            maxParticipants: '<%= t("test:max_participants") %>',
            qrCodeNotAvailable: '<%= t("test:qr_code_not_available") %>',
            instructionsForParticipants: '<%= t("test:instructions_for_participants") %>',
            accessViaLink: '<%= t("test:access_via_link") %>',
            enterTestCode: '<%= t("test:enter_test_code") %>',
            waitInLobby: '<%= t("test:wait_in_lobby") %>',
            joinAnytimeScheduled: '<%= t("test:join_anytime_scheduled") %>',
            startTestNow: '<%= t("test:start_test_now") %>',
            close: '<%= t("common:close") %>',
            joinLinkCopied: '<%= t("test:join_link_copied") %>'
        };
    </script>
    
    <!-- Room Management Scripts -->
    <script>
        // Function to switch department
        function switchDepartment() {
            Swal.fire({
                title: window.translations.switchDepartmentConfirm,
                text: window.translations.switchDepartmentText,
                icon: 'question',
                showCancelButton: true,
                confirmButtonColor: '#667eea',
                cancelButtonColor: '#64748b',
                confirmButtonText: window.translations.yesSwitchBtn,
                cancelButtonText: window.translations.cancelBtn
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch('/auth/api/admin/switch-room', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            window.location.href = data.redirectUrl;
                        } else {
                            throw new Error(data.message);
                        }
                    })
                    .catch(error => {
                        Swal.fire({
                            icon: 'error',
                            title: window.translations.errorTitle,
                            text: window.translations.errorSwitchDepartment,
                            confirmButtonColor: '#667eea'
                        });
                    });
                }
            });
        }

        // Add room info to existing functions
        const originalRefreshQuizzes = window.refreshQuizzes;
        window.refreshQuizzes = function() {
            showLoadingState(true);
            
            // Show room info in loading message
            const roomInfo = '<%= typeof roomInfo !== "undefined" && roomInfo ? roomInfo.name : "" %>';
            if (roomInfo) {
                document.querySelector('#loadingState p').textContent = window.translations.loadingQuizzes.replace('quizzes', roomInfo + ' quizzes');
            }
            
            setTimeout(() => {
                showLoadingState(false);
                showNotification(window.translations.quizzesRefreshed, 'success');
                
                // Re-animate cards
                document.querySelectorAll('.quiz-card').forEach((card, index) => {
                    card.style.animation = `fadeInUp 0.6s ease-out ${index * 0.1}s both`;
                });
            }, 1500);
        };

        // Override delete quiz function with translations
        window.deleteQuiz = async function(quizId) {
            try {
                const result = await Swal.fire({
                    title: window.translations.deleteQuizTitle,
                    text: window.translations.deleteQuizText,
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#ef4444',
                    cancelButtonColor: '#64748b',
                    confirmButtonText: window.translations.yesDeleteBtn,
                    cancelButtonText: window.translations.cancelBtn,
                    customClass: {
                        popup: 'swal-modern',
                        confirmButton: 'swal-confirm-modern',
                        cancelButton: 'swal-cancel-modern'
                    }
                });

                if (result.isConfirmed) {
                    // Show loading
                    Swal.fire({
                        title: window.translations.deletingQuiz,
                        text: window.translations.pleaseWait,
                        icon: 'info',
                        allowOutsideClick: false,
                        showConfirmButton: false,
                        customClass: {
                            popup: 'swal-modern'
                        }
                    });

                    const response = await fetch(`/quizzes/${quizId}`, {
                        method: 'DELETE',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });

                    if (!response.ok) {
                        throw new Error('Failed to delete quiz');
                    }

                    // Remove card with animation
                    const quizCard = document.querySelector(`[data-quiz-id="${quizId}"]`) || 
                                    document.querySelector(`.quiz-card:has(a[href*="${quizId}"])`);
                    
                    if (quizCard) {
                        quizCard.style.animation = 'fadeOut 0.5s ease-out';
                        setTimeout(() => {
                            quizCard.remove();
                            updateQuizCount();
                            applyFilters(); // Reapply filters to update counts
                        }, 500);
                    }

                    Swal.fire({
                        title: window.translations.quizDeleted,
                        text: window.translations.quizDeletedText,
                        icon: 'success',
                        timer: 3000,
                        timerProgressBar: true,
                        showConfirmButton: false,
                        customClass: {
                            popup: 'swal-modern'
                        }
                    });

                }
            } catch (error) {
                console.error('Delete quiz error:', error);
                Swal.fire({
                    title: window.translations.errorTitle,
                    text: window.translations.failedDeleteQuiz,
                    icon: 'error',
                    confirmButtonColor: '#667eea',
                    customClass: {
                        popup: 'swal-modern'
                    }
                });
            }
        };

        // Dropdown fix for room selection
        document.addEventListener('DOMContentLoaded', function() {
            
            if (typeof bootstrap === 'undefined') {
                console.error('Bootstrap JavaScript is not loaded!');
                return;
            }
            
            const dropdownElements = document.querySelectorAll('.dropdown-toggle');
            
            dropdownElements.forEach(function(element) {
                if (!element.hasAttribute('data-bs-toggle')) {
                    element.setAttribute('data-bs-toggle', 'dropdown');
                }
                if (!element.hasAttribute('aria-expanded')) {
                    element.setAttribute('aria-expanded', 'false');
                }
                
                try {
                    new bootstrap.Dropdown(element);
                } catch (error) {
                    console.error('Error initializing dropdown:', error);
                }
            });
        });
    </script>
    <script>
        // Create Test Modal functionality with translations
        document.addEventListener('DOMContentLoaded', function() {
            const createTestModal = new bootstrap.Modal(document.getElementById('createTestModal'));
            const testCreatedModal = new bootstrap.Modal(document.getElementById('testCreatedModal'));
            const createTestForm = document.getElementById('createTestForm');
            const createTestBtn = document.getElementById('createTestBtn');
            
            // Mode change handler
            document.querySelectorAll('input[name="mode"]').forEach(radio => {
                radio.addEventListener('change', function() {
                    const scheduleSettings = document.getElementById('scheduleSettings');
                    const startTimeInput = document.getElementById('startTime');
                    const endTimeInput = document.getElementById('endTime');
                    
                    if (this.value === 'offline') {
                        scheduleSettings.style.display = 'block';
                        startTimeInput.required = true;
                        endTimeInput.required = true;
                        
                        // Set default times (start: now + 1 hour, end: now + 24 hours)
                        const now = new Date();
                        const startTime = new Date(now.getTime() + 60 * 60 * 1000);
                        const endTime = new Date(now.getTime() + 24 * 60 * 60 * 1000);
                        
                        startTimeInput.value = startTime.toISOString().slice(0, 16);
                        endTimeInput.value = endTime.toISOString().slice(0, 16);
                    } else {
                        scheduleSettings.style.display = 'none';
                        startTimeInput.required = false;
                        endTimeInput.required = false;
                    }
                });
            });
            
            // Form submission
            createTestForm.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                const formData = new FormData(this);
                const data = {};
                
                for (let [key, value] of formData.entries()) {
                    data[key] = value;
                }
                
                // Add schedule settings for offline mode
                if (data.mode === 'offline') {
                    data.scheduleSettings = {
                        startTime: data.startTime,
                        endTime: data.endTime
                    };
                    delete data.startTime;
                    delete data.endTime;
                }
                
                // Show loading state
                createTestBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>' + window.translations.creatingTest;
                createTestBtn.disabled = true;
                
                try {
                    const response = await fetch('/test/create', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify(data)
                    });
                    
                    const result = await response.json();
                    
                    if (result.success) {
                        // Hide create modal
                        createTestModal.hide();
                        
                        // Show success modal
                        showTestCreatedModal(result);
                        
                        // Reset form
                        createTestForm.reset();
                        document.getElementById('scheduleSettings').style.display = 'none';
                    } else {
                        throw new Error(result.message);
                    }
                    
                } catch (error) {
                    console.error('Create test error:', error);
                    Swal.fire({
                        icon: 'error',
                        title: window.translations.errorCreatingTest,
                        text: error.message,
                        confirmButtonColor: '#667eea'
                    });
                } finally {
                    // Reset button
                    createTestBtn.innerHTML = '<i class="fas fa-rocket me-2"></i>' + window.translations.createTest;
                    createTestBtn.disabled = false;
                }
            });
            
            function showTestCreatedModal(result) {
                const { test, joinLink, qrCode } = result;
                const contentDiv = document.getElementById('testCreatedContent');
                const startTestBtn = document.getElementById('startTestBtn');
                
                let scheduleInfo = '';
                if (test.mode === 'offline' && test.scheduleSettings) {
                    const startTime = new Date(test.scheduleSettings.startTime);
                    const endTime = new Date(test.scheduleSettings.endTime);
                    scheduleInfo = `
                        <div class="alert alert-info">
                            <i class="fas fa-clock me-2"></i>
                            <strong>${window.translations.schedule}:</strong> ${startTime.toLocaleString()} - ${endTime.toLocaleString()}
                        </div>
                    `;
                }
                
                contentDiv.innerHTML = `
                    <div class="text-center mb-4">
                        <div class="badge bg-success fs-6 mb-3">
                            ${window.translations.testCode}: <strong>${test.testCode}</strong>
                        </div>
                        <h6>Quiz #${test.quizNumber}: ${test.quizTitle}</h6>
                        <p class="text-muted">${window.translations.mode}: ${test.mode.toUpperCase()} | ${window.translations.maxParticipants}: ${test.maxParticipants}</p>
                    </div>
                    
                    ${scheduleInfo}
                    
                    <div class="row">
                        <div class="col-md-12" style="text-align: center;">
                            ${qrCode ? `<img src="${qrCode}" alt="QR Code" class="img-fluid" style="width: 300px;">` : `<p class="text-muted">${window.translations.qrCodeNotAvailable}</p>`}
                        </div>
                        <div class="col-md-12">
                            <div class="input-group mb-3">
                                <input type="text" class="form-control" id="joinLinkInput" value="${joinLink}" readonly>
                                <button class="btn btn-outline-primary" onclick="copyJoinLink()">
                                    <i class="fas fa-copy"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                    
                    <div class="alert alert-warning mt-3">
                        <i class="fas fa-info-circle me-2"></i>
                        <strong>${window.translations.instructionsForParticipants}</strong>
                        <ul class="mb-0 mt-2">
                            <li>${window.translations.accessViaLink}</li>
                            <li>${window.translations.enterTestCode}: <strong>${test.testCode}</strong></li>
                            ${test.mode === 'online' ? `<li>${window.translations.waitInLobby}</li>` : `<li>${window.translations.joinAnytimeScheduled}</li>`}
                        </ul>
                    </div>
                `;
                
                // Show/hide start test button for online mode
                if (test.mode === 'online') {
                    startTestBtn.style.display = 'inline-block';
                    startTestBtn.onclick = function() {
                        const adminUrl = `/test/room/${test.testCode}?admin=true`;
                        window.open(adminUrl, '_blank');
                        testCreatedModal.hide();
                    };
                } else {
                    startTestBtn.style.display = 'none';
                }
                
                testCreatedModal.show();
            }
            
            // Global function for copying join link
            window.copyJoinLink = function() {
                const input = document.getElementById('joinLinkInput');
                input.select();
                document.execCommand('copy');
                
                // Show feedback
                const button = event.target.closest('button');
                const originalContent = button.innerHTML;
                button.innerHTML = '<i class="fas fa-check text-success"></i>';
                
                setTimeout(() => {
                    button.innerHTML = originalContent;
                }, 2000);
                
                showNotification(window.translations.joinLinkCopied, 'success');
            };
            
            // Global function to open create test modal
            window.openCreateTestModal = function() {
                createTestModal.show();
            };
        });
        </script>
</body>
</html>