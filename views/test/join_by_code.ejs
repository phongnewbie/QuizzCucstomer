<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <!-- SweetAlert2 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/@sweetalert2/theme-bootstrap-4/bootstrap-4.css" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #667eea;
            --secondary-color: #764ba2;
            --success-color: #10b981;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
            --info-color: #3b82f6;
            --dark-color: #1e293b;
            --light-color: #f8fafc;
            --border-radius: 16px;
            --shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 25px 50px rgba(0, 0, 0, 0.15);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.05) 0%, transparent 50%);
            pointer-events: none;
            z-index: -1;
        }

        .join-code-container {
            width: 100%;
            max-width: 480px;
            margin: 0 auto;
        }

        .join-code-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-lg);
            overflow: hidden;
            transition: var(--transition);
        }

        .join-code-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.15);
        }

        .card-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            padding: 2.5rem 2rem;
            text-align: center;
            border: none;
            position: relative;
        }

        .card-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 150px;
            height: 150px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(25%, -25%);
        }

        .header-icon {
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2rem;
            position: relative;
            z-index: 2;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .card-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            position: relative;
            z-index: 2;
            letter-spacing: -0.025em;
        }

        .card-subtitle {
            font-size: 1.1rem;
            opacity: 0.9;
            font-weight: 400;
            line-height: 1.5;
            margin: 0;
            position: relative;
            z-index: 2;
        }

        .card-body {
            padding: 2.5rem 2rem;
        }

        .code-input-section {
            text-align: center;
            margin-bottom: 2rem;
        }

        .code-input-title {
            color: var(--dark-color);
            font-weight: 600;
            font-size: 1.2rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .code-input {
            background: #f8fafc;
            border: 3px solid #e2e8f0;
            border-radius: 16px;
            padding: 1.5rem;
            font-size: 2rem;
            font-weight: 700;
            text-align: center;
            letter-spacing: 0.5rem;
            width: 100%;
            max-width: 300px;
            margin: 0 auto 1rem;
            transition: var(--transition);
            font-family: 'Courier New', monospace;
        }

        .code-input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
            background: white;
        }

        .code-input::placeholder {
            color: #94a3b8;
            font-weight: 400;
            letter-spacing: 0.25rem;
        }

        .input-help {
            color: #64748b;
            font-size: 0.875rem;
            margin-bottom: 1.5rem;
        }

        .btn-join-test {
            background: linear-gradient(135deg, var(--success-color) 0%, #059669 100%);
            border: none;
            border-radius: 12px;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            color: white;
            width: 100%;
            transition: var(--transition);
            box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
            position: relative;
            overflow: hidden;
        }

        .btn-join-test::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-join-test:hover::before {
            left: 100%;
        }

        .btn-join-test:hover {
            background: linear-gradient(135deg, #059669 0%, #047857 100%);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
            color: white;
        }

        .btn-join-test:disabled {
            background: #94a3b8;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .info-section {
            background: linear-gradient(135deg, #f0f7ff 0%, #e0f2fe 100%);
            border: 1px solid #bae6fd;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .info-title {
            color: var(--info-color);
            font-weight: 600;
            font-size: 1rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .info-list {
            color: #475569;
            font-size: 0.9rem;
            margin: 0;
            padding-left: 1.5rem;
        }

        .info-list li {
            margin-bottom: 0.5rem;
        }

        .user-info {
            background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
            border: 1px solid #fbbf24;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .user-info .user-name {
            font-weight: 700;
            color: var(--warning-color);
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .user-info .user-email {
            font-size: 0.9rem;
            color: #92400e;
            font-weight: 500;
        }

        .footer-links {
            text-align: center;
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid #e2e8f0;
        }

        .footer-links a {
            color: var(--primary-color);
            text-decoration: none;
            font-size: 0.9rem;
            margin: 0 1rem;
            transition: var(--transition);
            font-weight: 500;
        }

        .footer-links a:hover {
            color: var(--secondary-color);
            text-decoration: underline;
        }

        .fade-in {
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Digit animation for code input */
        .code-input.typing {
            animation: typing 0.3s ease;
        }

        @keyframes typing {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

        @media (max-width: 576px) {
            .join-code-container {
                padding: 0 10px;
            }
            
            .card-header {
                padding: 2rem 1.5rem;
            }
            
            .card-body {
                padding: 2rem 1.5rem;
            }
            
            .card-title {
                font-size: 1.75rem;
            }
            
            .code-input {
                font-size: 1.75rem;
                padding: 1.25rem;
                letter-spacing: 0.25rem;
            }
        }
    </style>
</head>
<body>
    <div class="join-code-container fade-in">
        <div class="card join-code-card">
            <div class="card-header">
                <div class="header-icon">
                    <i class="fas fa-key"></i>
                </div>
                <h1 class="card-title">Enter Test Code</h1>
                <p class="card-subtitle">Join a test session using your 6-digit code</p>
            </div>
            
            <div class="card-body">
                <!-- User Info (if authenticated) -->
                <% if (typeof user !== 'undefined' && user) { %>
                <div class="user-info">
                    <div class="user-name">
                        <i class="fas fa-user-circle"></i>
                        <span><%= user.name %></span>
                    </div>
                    <div class="user-email"><%= user.email %></div>
                </div>
                <% } %>

                <!-- Instructions -->
                <div class="info-section">
                    <div class="info-title">
                        <i class="fas fa-info-circle"></i>
                        How to join a test:
                    </div>
                    <ol class="info-list">
                        <li>Get the 6-digit test code from your instructor or test administrator</li>
                        <li>Enter the code in the field below</li>
                        <li>Click "Join Test" to access the test session</li>
                        <li>Follow the on-screen instructions to participate</li>
                    </ol>
                </div>

                <!-- Code Input Form -->
                <form id="joinCodeForm">
                    <div class="code-input-section">
                        <div class="code-input-title">
                            <i class="fas fa-hashtag"></i>
                            Test Code
                        </div>
                        <input type="text" 
                               class="code-input" 
                               id="testCode" 
                               name="testCode"
                               placeholder="000000" 
                               maxlength="6"
                               pattern="[0-9]{6}"
                               autocomplete="off"
                               required>
                        <div class="input-help">
                            Enter the 6-digit code provided by your instructor
                        </div>
                    </div>

                    <button type="submit" class="btn btn-join-test" id="joinTestBtn" disabled>
                        <i class="fas fa-sign-in-alt me-2"></i>
                        Join Test
                    </button>
                </form>

                <!-- Footer Links -->
                <div class="footer-links">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <% if (user.role === 'admin') { %>
                            <a href="/quizzes">
                                <i class="fas fa-tachometer-alt me-1"></i>Dashboard
                            </a>
                        <% } else { %>
                            <a href="/player/dashboard">
                                <i class="fas fa-home me-1"></i>Dashboard
                            </a>
                        <% } %>
                    <% } %>
                    <a href="/auth/logout">
                        <i class="fas fa-sign-out-alt me-1"></i>Switch Account
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('joinCodeForm');
            const codeInput = document.getElementById('testCode');
            const joinBtn = document.getElementById('joinTestBtn');

            // Code input formatting and validation
            codeInput.addEventListener('input', function(e) {
                // Remove non-digits
                let value = e.target.value.replace(/\D/g, '');
                
                // Limit to 6 digits
                if (value.length > 6) {
                    value = value.slice(0, 6);
                }
                
                e.target.value = value;
                
                // Add typing animation
                e.target.classList.add('typing');
                setTimeout(() => {
                    e.target.classList.remove('typing');
                }, 300);
                
                // Validate form
                validateForm();
            });

            // Paste handling
            codeInput.addEventListener('paste', function(e) {
                e.preventDefault();
                const paste = (e.clipboardData || window.clipboardData).getData('text');
                const numericValue = paste.replace(/\D/g, '').slice(0, 6);
                this.value = numericValue;
                
                // Trigger input event to validate
                this.dispatchEvent(new Event('input'));
            });

            // Form validation
            function validateForm() {
                const code = codeInput.value;
                const isValid = code.length === 6;
                
                joinBtn.disabled = !isValid;
                
                // Visual feedback
                if (code.length > 0) {
                    if (isValid) {
                        codeInput.style.borderColor = '#10b981';
                        codeInput.style.backgroundColor = '#f0fdf4';
                    } else {
                        codeInput.style.borderColor = '#f59e0b';
                        codeInput.style.backgroundColor = '#fffbeb';
                    }
                } else {
                    codeInput.style.borderColor = '#e2e8f0';
                    codeInput.style.backgroundColor = '#f8fafc';
                }
                
                return isValid;
            }

            // Form submission
            form.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                if (!validateForm()) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Invalid Code',
                        text: 'Please enter a valid 6-digit test code.',
                        confirmButtonColor: '#667eea'
                    });
                    return;
                }

                const testCode = codeInput.value;

                // Show loading state
                const originalContent = joinBtn.innerHTML;
                joinBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Finding Test...';
                joinBtn.disabled = true;

                try {
                    const response = await fetch('/test/join', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ testCode })
                    });

                    const result = await response.json();

                    if (result.success) {
                        // Show success message
                        Swal.fire({
                            icon: 'success',
                            title: 'Test Found!',
                            text: 'Redirecting to test session...',
                            timer: 1500,
                            timerProgressBar: true,
                            showConfirmButton: false
                        }).then(() => {
                            // Redirect to test
                            window.location.href = result.redirectUrl;
                        });
                    } else {
                        throw new Error(result.message);
                    }

                } catch (error) {
                    console.error('Join test error:', error);
                    
                    // Show error message
                    Swal.fire({
                        icon: 'error',
                        title: 'Test Not Found',
                        text: error.message || 'Please check your code and try again.',
                        confirmButtonColor: '#667eea'
                    });
                    
                    // Clear input and focus
                    codeInput.value = '';
                    codeInput.focus();
                    validateForm();
                    
                } finally {
                    // Reset button
                    joinBtn.innerHTML = originalContent;
                    validateForm();
                }
            });

            // Auto-focus on code input
            codeInput.focus();

            // Keyboard shortcuts
            document.addEventListener('keydown', function(e) {
                // Escape to clear form
                if (e.key === 'Escape') {
                    codeInput.value = '';
                    codeInput.focus();
                    validateForm();
                }
                
                // Enter to submit if valid
                if (e.key === 'Enter' && validateForm() && !joinBtn.disabled) {
                    form.dispatchEvent(new Event('submit'));
                }
            });

            // Number pad support
            document.addEventListener('keydown', function(e) {
                // Allow numeric keys, backspace, delete, tab, escape, enter
                const allowedKeys = [
                    'Backspace', 'Delete', 'Tab', 'Escape', 'Enter',
                    'ArrowLeft', 'ArrowRight', 'Home', 'End'
                ];
                
                const isNumber = /^[0-9]$/.test(e.key);
                const isAllowed = allowedKeys.includes(e.key);
                
                if (document.activeElement === codeInput && !isNumber && !isAllowed) {
                    e.preventDefault();
                }
            });

            // Animate on load
            setTimeout(() => {
                codeInput.style.transform = 'scale(1.02)';
                setTimeout(() => {
                    codeInput.style.transform = 'scale(1)';
                }, 200);
            }, 500);
        });
    </script>
</body>
</html>